#!/bin/sh
#
# Run commandline support through SymmetricLauncher
#

PRGDIR=`dirname "$0"`
LIBDIR=$PRGDIR/../lib
RUNJAVA=java

if [ -x "$JAVA_HOME/bin/java" ]
then
   RUNJAVA="$JAVA_HOME/bin/java"
fi

exec "$RUNJAVA" -Dlog4j.configuration=file:log4j.xml -jar "$LIBDIR/symmetric-ds-1.1.0.jar" "$@"
